# Custom Packages

This directory is reserved for custom Nix packages specific to this dotfiles repository.

Currently, all packages are sourced from upstream flake inputs, so this directory is empty.
